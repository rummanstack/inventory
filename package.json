{
  "name": "arinda-inventory-system-root",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix backend test",
    "lambda-build": "node scripts/lambda-build.mjs",
    "install-client": "npm install --prefix frontend",
    "install-server": "npm install --prefix backend",
    "install": "npm run install-client && npm run install-server",
    "client": "npm run dev --prefix frontend",
    "server": "npm run dev --prefix backend",
    "watch": "concurrently \"npm run client\" \"npm run server\"",
    "deploy": "npm run backend-build --prefix frontend && npm start --prefix backend",
    "deploy-cluster": "npm run backend-build --prefix frontend && npm run cluster --prefix backend",
    "deploy-watch": "npm run backend-build --prefix frontend && npm run watch --prefix backend",
    "render-build": "node scripts/render-build.mjs",
    "start": "npm run start --prefix backend",
    "test:dealer-check": "npm --prefix backend run test:dealer-check"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
